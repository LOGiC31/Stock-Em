<!DOCTYPE html>
<html lang="en">
<head>
 <meta charset="UTF-8">
 <meta name="viewport" content="width=device-width, initial-scale=1.0">
 <title>Stock'Em - <%= @item.item_name %></title>
 <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
 <style>
   body {
     font-family: Arial, sans-serif;
     margin: 0;
     padding: 0;
     background-color: #f8f8f8;
     display: flex;
     flex-direction: column;
     min-height: 100vh;
   }
   header {
     display: flex;
     justify-content: space-between;
     align-items: center;
     background-color: #500000;
     color: white;
     padding: 15px 20px;
   }
   .header-title {
     font-size: 30px;
     font-weight: bold;
   }
   .greeting {
     font-size: 20px;
     margin-right: 10px;
   }
   .user-icon {
     font-size: 20px;
     color: white;
   }
   main {
     padding: 40px 100px;
     flex: 1;
     background-color: #fff;
     border-radius: 10px;
     box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
     margin: 20px auto;
     max-width: 1200px;
   }
   h1 {
     text-align: center;
     color: #500000;
     margin-bottom: 30px;
   }
   .content-wrapper {
     display: flex;
     justify-content: space-between;
     gap: 40px;
     align-items: flex-start;
   }
   .left-column, .right-column {
     flex: 1;
     padding: 20px;
     background-color: #f5f5f5;
     border-radius: 10px;
     box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
     flex-shrink: 0;
   }
   h2 {
     color: #500000;
     margin-bottom: 15px;
     font-size: 22px;
   }
   p {
     font-size: 16px;
     color: #666;
     margin-bottom: 10px;
   }
   .item-details strong {
     color: #333;
   }
   .availability {
     font-weight: bold;
   }
   .availability span {
     font-size: 16px;
   }
   img {
     margin: 20px 0;
     max-width: 100%;
     height: auto;
     border-radius: 10px;
     box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
   }
   .btn-container {
     margin-top: 20px;
     display: flex;
     gap: 10px;
   }
   .btn {
     padding: 10px 20px;
     background-color: #500000;
     color: white;
     border-radius: 20px;
     text-decoration: none;
     font-weight: bold;
     transition: background-color 0.3s;
     box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
   }
   .btn2 {
     font-size: 14px;
     padding: 5px 10px;
     background-color: #9c7c08;
     color: white;
     border-radius: 10px;
     font-weight: bold;
     text-decoration: none;
     transition: background-color 0.3s;
     box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
   }
   .btn:hover {
     background-color: #800000;
   }
   footer {
     display: flex;
     justify-content: space-between;
     align-items: center;
     background-color: #500000;
     color: white;
     padding: 15px 20px;
     margin-top: auto;
   }
 </style>
</head>
<body>


<header>
 <div class="header-title">Stock'Em</div>
 <div>
   <span class="greeting">Hello, <span id="username">User</span></span>
   <span class="icon user-icon"><i class="fas fa-user"></i></span>
 </div>
</header>


<main>
 <h1><%= @item.item_name %></h1>
  <div class="content-wrapper">
   <div class="left-column">
     <h2>Item Details</h2>
    
     <div class="item-details">
       <p><strong>Item ID:</strong> <%= @item.id %></p>
       <p><strong>Serial Number:</strong> <%= @item.serial_number %></p>
       <p><strong>Category:</strong> <%= @item.category %></p>
       <p><strong>Quality Score:</strong> <%= @item.quality_score %></p>
       <p><strong>Available:</strong>
         <span class="availability" style="color: <%= @item.currently_available ? 'green' : 'red' %>;">
           <%= @item.currently_available ? 'Yes' : 'No' %>
         </span>
       </p>
       <p><strong>Details:</strong> <%= @item.details %></p>
     </div>
    
     <%= image_tag(@item.image) if @item.image.present? %>
    
     <div class="btn-container">
       <%= link_to 'Edit', edit_item_path(@item), class: 'btn' %>
       <%= link_to 'Back to Items', items_path, class: 'btn' %>
     </div>
   </div>


   <div class="right-column">
     <h2>Item History</h2>
     <p>Checkin on 8/21/2024</p>
     <p>Returned by Mark Sturtevant</p>
     <p>Item given to Professor Wade</p>
     <p>New Approximate Location: Wade's Attic</p>
     <p>Details: Now has water damage :(</p>


     <h2>Notes</h2>
	 
	 <!-- Add in "create note button if have not done so already -->
	 
	 <% if !@writing_note %>
	 <%= link_to 'Create Note', item_path(id: @item.id, writing_note: true), class: 'btn2' %>
	 <% end %>
	 
	 <% if @writing_note %>
	 <%= form_tag( add_note_path, method: "get") do %>
		<%= label_tag(:note_msg, '') %>
		<%= text_area_tag(:note_msg) %>
		<%= hidden_field_tag :id, @item.id %>
		<br>
		<input type="submit" value="Publish", class: 'btn2'>
	 <% end %>
	 <% end %>
	 
	 <br>
	 
	 <% @notes.each do |note| %>
		<br>
        <%= note.user_id%> <%= '(' %> <%= note.created_at %> <%= ')' %>
		<br>
		<%= note.msg %>
		<br>
	 <% end %>
	 
   </div>
 </div>
</main>


<footer>
 <div>The Stock'Em Team, 2024</div>
</footer>


</body>
</html>
